@page "/Serials/editCarguesDatails/{Id:guid}"
@attribute [Authorize(Roles = "Usuario")]

@if (CargueDetail is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <FormSerialsDetails CargueDetail="CargueDetail"
                             IsEditControl="true"
                             OnSubmit="Edit"
                             ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}
